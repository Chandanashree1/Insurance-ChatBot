CREATE TABLE PRODUCT (
    PRODUCT_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    PRODUCT_NAME VARCHAR2(100) NOT NULL,
    PRODUCT_TYPE VARCHAR2(30) NOT NULL,
    DESCRIPTION VARCHAR2(500),
    IS_ACTIVE CHAR(1) DEFAULT 'Y' NOT NULL,

    CONSTRAINT PK_PRODUCT
        PRIMARY KEY (PRODUCT_ID),

    CONSTRAINT UK_PRODUCT_TYPE
        UNIQUE (PRODUCT_TYPE),

    CONSTRAINT CK_PRODUCT_ACTIVE
        CHECK (IS_ACTIVE IN ('Y', 'N'))
);
INSERT INTO PRODUCT (
    PRODUCT_NAME,
    PRODUCT_TYPE,
    DESCRIPTION
)
VALUES (
    'Comprehensive Insurance',
    'COMPREHENSIVE',
    'Motor insurance with comprehensive coverage'
);

INSERT INTO PRODUCT (
    PRODUCT_NAME,
    PRODUCT_TYPE,
    DESCRIPTION
)
VALUES (
    'Third Party Insurance',
    'THIRD_PARTY',
    'Motor insurance with third party coverage'
);

COMMIT;
SELECT *
FROM PRODUCT;
SELECT PRODUCT_ID, PRODUCT_NAME, PRODUCT_TYPE
FROM PRODUCT;