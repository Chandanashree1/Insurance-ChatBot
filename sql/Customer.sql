CREATE TABLE CUSTOMER (
    CUSTOMER_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    MOBILE_NUMBER VARCHAR2(20) NOT NULL,
    FULL_NAME VARCHAR2(100) NOT NULL,
    CIVIL_ID_LICENSE_NO VARCHAR2(30) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,

    CONSTRAINT PK_CUSTOMER
        PRIMARY KEY (CUSTOMER_ID)
);

ALTER TABLE CUSTOMER
ADD (
    EMAIL VARCHAR2(150)
);

ALTER TABLE CUSTOMER
ADD (
    PASSWORD VARCHAR2(255)
);

-- Check identity column
SELECT sequence_name
FROM user_tab_identity_cols
WHERE table_name = 'CUSTOMER';

-- Unique email
ALTER TABLE CUSTOMER
ADD CONSTRAINT UQ_CUSTOMER_EMAIL UNIQUE (EMAIL);

-- Allow NULL values
ALTER TABLE CUSTOMER MODIFY MOBILE_NUMBER NULL;
ALTER TABLE CUSTOMER MODIFY CIVIL_ID_LICENSE_NO NULL;

-- Insert customer
INSERT INTO CUSTOMER (
    MOBILE_NUMBER,
    FULL_NAME,
    CIVIL_ID_LICENSE_NO,
    EMAIL
)
VALUES (
    '91111111',
    'Chandana',
    '62029585',
    'chandana@gmail.com'
);

COMMIT;

-- Check customers
SELECT CUSTOMER_ID, FULL_NAME
FROM CUSTOMER;

SELECT *
FROM CUSTOMER;

SELECT CUSTOMER_ID, FULL_NAME, MOBILE_NUMBER
FROM CUSTOMER
ORDER BY CUSTOMER_ID;

-- Verify table name
SELECT table_name
FROM user_tables
WHERE table_name IN ('CUSTOMER', 'CUSTOMERS');

DROP TABLE CUSTOMER CASCADE CONSTRAINTS;